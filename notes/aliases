
              ██ ██
             ░██░░
    ██████   ░██ ██  ██████    ██████  █████   ██████
   ░░░░░░██  ░██░██ ░░░░░░██  ██░░░░  ██░░░██ ██░░░░
    ███████  ░██░██  ███████ ░░█████ ░███████░░█████
   ██░░░░██  ░██░██ ██░░░░██  ░░░░░██░██░░░░  ░░░░░██
  ░░████████ ███░██░░████████ ██████ ░░██████ ██████
   ░░░░░░░░ ░░░ ░░  ░░░░░░░░ ░░░░░░   ░░░░░░ ░░░░░░

Shortcuts:
    cat - 'bat'
    _ - 'sudo'
    g - 'git'
    h - 'history'
    hf <text> - 'history | grep -i', search history for text
    cppcompile - 'c++ -std=c++11 -stdlib=libc++', compile files in C++11
    newreact - 'npx create-react-app'
    
Terminal Utilities:
    permex <target> - add permisson to execute to a file
    symlink <target> <destination> [-f -r][--force --remove] - a shortcut for symlinking files with ln
    prepend-path <target> - add something to the path
    cdf - change working directory to the top-most Finder window location
    cpwd - copy current absolute directory path
    get <var> - get named variable
    ip - get machine's IP address
    printpath - get the PATH printed on seperate lines
    f <keyword> - fast, recursive search for file name in directories
    
System:
    desktopshow/desktophide - show/hide desktop icons
    showhidden/hidehidden - show/hide hidden files
    spoton/spotoff - turn spotlight search on/off
    ql <target> - quick look file from command line
    osupdate - update macOS
    emptytrash - empty system trash and clear files
    afk - put the screen to sleep
    badge - Ring the terminal bell (useful after long commands)
    ios - open iOS simulator

Wifi:
    transfer <target> - trasnfer a file using trasnfer.sh
    whois <url> - get whois record for domain
    getcertnames <url> - Show all names in SSL cert for a given domain
    airport - shortcut to the Airport CLI
    wifis - list nearby wifi networks
    
Configs:
    gitconfig - open ~/.gitconfig
    zshconfig - open ~/.zshrc
    zshlocal - open ~/.zshrc_local
    
Encryption:
    encrypt [create] <target> [pass] [size] - create an encrypted image or encrypt a folder
    decrypt <target> [pass] - decrypt an image, attach it and cd in
    detach - detach a volume; also useable on external drives. Can also be calleed with 'eject'

Clear/Reset:
    reload - source ~/.zshrc
    nomnom - killall
    cleartest - clear the test directory
    flushdns - flush the DNS
    killscreens - kill all detached screens
    killfinder - quit and reloads Finder
    killdock - quits and reloads the dock
    killmenubar - quits and reloads the menu bar
    killos - quits and reloads all three
    
    
Useful Plugin Aliases:

    acs - view all aliases provied by plugins
    wd - warp directory
        wd add [target] - add a warp point at the current/target directory
        wd rm [target] - remove a warp point in the current/target directory
        wd ..\... - return to the previous directory
        wd list - list warp points
    dirhist - show the last 20 unique directories visited
    <searchengine> <terms> - search the web using the command line
    compress <format> <files> - compress the files into an archive
    extract <archive> - extract a number of different archive formats
    pp_json - pretty print a .json file
    is_json - returns true if valid json; false otherwise
    ofd - open the current directory in a Finder window
    pfd - return the path of the frontmost Finder window
    pfs - return the current Finder selection
    cdx - cd to the current Xcode project directory
    man-preview - open a specified man page in Preview app
    spotify <command> - control and search Spotify; use "spotify help" for usage
    btrestart - Restart the Bluetooth daemon
    gi list - list .gitignore templates
    gi [TEMPLATENAME] >> .gitignore - add .gitignore of template to the directory
    genpass - generate a secure password
    sm [target] - launch sublime-merge, opening the targetr if there is one
    
Scripts:
    
    pfetch - fetch system infomation with pfetch
    colorscheme - display the main terminal colorscheme in various combinations
    uu - universal update
    

Folder Aliases:
    
    dl - ~/Downloads
    dt - ~/Desktop
    space - ~/Workspace
    _space - ~/Workspace/__space__
    projects - ~/Workspace/projects
    test - ~/Workspace/.test
    dotfiles - ~/Workspace/__space__/.dotfiles
    
Application Aliases:

    chrome
    firefox
    safari
    plistbuddy
    finder
    xcode
    sublime
    fontbook
    
